services:
  docs:
    image: ort_ims_local_docs
    container_name: ort_ims_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./ort_ims:/app/ort_ims:z
    ports:
      - '9000:9000'
    command: /start-docs
